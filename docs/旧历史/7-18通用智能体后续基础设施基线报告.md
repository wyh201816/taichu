# 通用智能体后续基础设施基线报告

> 更新日期：2026-07-18  
> 阶段：`P00`  
> 任务包：`太初-通用Agent后续推进任务包-20260717`  
> 结论：`PASS`

本报告只冻结 2026-07-18 在当前仓库和本机生产配置上实际执行得到的基线，不定义新的架构规则，也不复制完整 Prompt、模型回答或小说正文。`PASS` 表示阶段 00 的审计、复现和缺口冻结已完成且无需重定基线，不表示 8 个效果样例全部通过。

## 1. 仓库状态

- HEAD：`7ad671f1f2558c39558c004e17b6d004b1a6b083`，与任务包目标提交完全一致，无新增提交需要重放。
- 分支：`master`。
- 远程：`taichu` → `https://github.com/qingluo201816/taichu.git`。
- 任务包校验：`validate_task_pack.py` 通过，共 9 个阶段、79 个任务；`checksums.sha256` 逐项校验通过。
- 任务包的 `check_repository_baseline.py` 未通过，原因是脚本固定读取名为 `origin` 的远程，而本仓库的正确远程名为 `taichu`。已用 `git rev-parse HEAD`、`git branch --show-current` 和 `git remote -v` 等价人工复核，仓库地址与提交均匹配；不为适配脚本改名远程。
- 阶段开始前已有并已保护的用户修改：
  - `web/src/components/agent-task-monitor/task-monitor-overview.tsx`
  - `web/src/components/agent-workbench/general-agent-workbench.tsx`
- 任务包目录本身为未跟踪输入资料。本阶段新增历史报告并更新资料入口；真实运行同时生成了本地 `project_assets/derived/` 运行、调用、评测与中间产物，未改动生产源码或事实源。
- `project_assets/source/knowledge/` 不存在；仓库未发现 `.gitkeep`。

## 2. 完成任务

| 任务 ID | 状态 | 结果 |
|---|---|---|
| `BAS-001` | 完成 | HEAD、分支、远程、工作树、关键目录、废弃目录和 `.gitkeep` 已核对 |
| `BAS-002` | 完成 | 后端依赖、静态检查、类型检查、全量测试和编译门禁全部通过 |
| `BAS-003` | 完成 | 前端依赖、Lint、类型检查、专项测试和生产构建全部通过 |
| `BAS-004` | 完成 | 固定端口、核心 API、四个页面、浏览器控制台及 MongoDB 失败语义已验证 |
| `BAS-005` | 完成 | 8 个核心评测样例及直接回答、并行、澄清、写入授权、失败恢复样例已真实执行 |
| `BAS-006` | 完成 | 召回、向量、记忆、路由、恢复、语义评测与长期经验缺口已映射到后续阶段 |
| `BAS-007` | 完成 | 本报告、仓库地图和已确认架构决策状态已同步 |

## 3. 当前能力与目录事实

关键入口均存在且职责未漂移：

- `src/taichu/application/retrieval/`
- `src/taichu/application/contracts/retrieval.py`
- `src/taichu/application/services/retrieval_service.py`
- `src/taichu/infrastructure/retrieval/`
- `src/taichu/application/tools/`
- `src/taichu/application/subagents/`
- `src/taichu/application/general_agent/`
- `src/taichu/infrastructure/general_agent_runs/`
- `src/taichu/application/evaluations/general_agent/`

生产发现结果为 16 个 Tool 和 12 个专业子 Agent：

- Tool：`retrieve_knowledge`、`resolve_knowledge_identity`、`list_knowledge_catalog`、`read_knowledge_cards`、`read_manuscript`、`search_manuscript`、`preview_manuscript_patch`、`apply_manuscript_patch`、`get_novel_structure`、`create_novel_structure_items`、`update_novel_structure`、`delete_novel_structure_items`、`create_confirmed_knowledge`、`update_confirmed_knowledge`、`search_external_sources`、`read_external_source`。
- 子 Agent：`canon_evidence`、`character`、`worldbuilding`、`story_architecture`、`scene_planning`、`drafting`、`revision`、`consistency_reviewer`、`narrative_reviewer`、`style_reviewer`、`narrative_summary`、`external_research`。

## 4. 可复现门禁

### 4.1 环境版本

| 组件 | 版本 |
|---|---|
| Python | 3.12.7 |
| uv | 0.11.7 |
| MongoDB | 8.3.4 |
| Node.js | v24.14.0 |
| npm | 11.9.0 |

### 4.2 后端

| 命令 | 结果 | 实测耗时或摘要 |
|---|---|---|
| `uv sync` | 通过 | 解析 60 个包、检查 59 个包，约 0.05 秒 |
| `uv run ruff check src tests` | 通过 | 约 0.16 秒 |
| `uv run mypy` | 通过 | 313 个源文件，约 2.02 秒 |
| `uv run pytest -q` | 通过 | 297 passed、1 skipped、23 subtests passed；pytest 24.17 秒，外层约 25.92 秒 |
| `uv run python -m compileall -q src` | 通过 | 约 0.28 秒 |
| `git diff --check` | 通过 | 初始基线约 0.09 秒，仅对两处既有前端修改提示 LF/CRLF 转换风险；最终文档与运行产物自检仍无空白错误 |

### 4.3 前端

| 命令 | 结果 | 实测耗时或摘要 |
|---|---|---|
| `npm ci` | 通过 | 647 个包，约 49.63 秒；npm 报告 2 个中危依赖告警 |
| `npm run lint` | 通过 | 约 9.76 秒 |
| `npx tsc --noEmit` | 通过 | 约 4.22 秒 |
| `npm run test:general-agent` | 通过 | 3 组通用 Agent 展示、节点监控和评测测试，约 2.21 秒 |
| `npm run test:llm` | 通过 | 约 2.12 秒 |
| `npm run build` | 通过 | Next.js 16.2.9，18 个静态页面，约 13.15 秒 |

生产构建包含 `/agent-workbench`、`/task-monitor/general-agent`、`/task-monitor/general-agent/evaluation` 和 `/model-monitor`。

## 5. 固定端口与核心链路

- 使用根目录 `start.bat` 和 `TAICHU_NON_INTERACTIVE=1` 成功启动或复用 MongoDB `27017`、后端 `8000`、前端 `3000`。
- 后端 `/openapi.json`、通用 Agent 运行列表、通用 Agent 评测集和模型列表 API 均返回 200。
- 在应用内浏览器检查工作台、节点监控、通用 Agent 评测和模型监控四个页面，页面可交互，控制台无新增错误或警告。
- 评测页首次访问命中了旧 `.next/dev` 缓存并返回 404；触发该路由重新编译后恢复 200，生产构建也确认该路由存在。未因此改动源文件。
- 主动正常停止 MongoDB 后，有效知识卡列表请求明确返回 503，没有回退到 JSON；再次运行 `start.bat` 后恢复 200。
- 评测页存在一个当前展示问题：默认选中的样例与页面显示的历史评测、实际回答和参考答案不一致。该问题仅记录基线，阶段 00 未修改前端。

## 6. 八个核心样例基线

数据集：`general_writing_assistant_core`  
校验和：`d1b4d6b92be407e9ecad79ce4b458bfe498270f567e168c0d6ab00596945f9e9`  
模型：`deepseek-v4-pro`  
重复次数：每个样例 1 次

表中“来源”按运行节点记录的去重来源引用计数；“召回”依次为调用数/候选数/命中数/总耗时。

| 样例 | 运行状态与能力路径 | 节点/重规划 | 来源 | 耗时 | Token / 估算成本 | 召回 | 检查点 | 得分 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| `fact_xiaojin_growth`<br>`general_run_20260718_005219_946ba0` | 完成；`canon_evidence` | 1/0 | 2 | 65.83 秒 | 22,704 / ¥0.081321 | 1/39/1/7 ms | 10 | 94，通过 |
| `direct_conflict_advice`<br>`general_run_20260718_005325_1762a7` | 完成；直接回答，无 DAG 节点 | 0/0 | 0 | 15.45 秒 | 14,307 / ¥0.045171 | 0/0/0/0 ms | 8 | 91，通过 |
| `character_xiaojin_risk`<br>`general_run_20260718_005341_24f2e6` | 完成；`canon_evidence` | 1/0 | 14 | 51.33 秒 | 33,625 / ¥0.109497 | 1/39/12/5 ms | 10 | 97，通过 |
| `plan_xiaojin_conflict`<br>`general_run_20260718_005432_c80e5d` | 完成；`story_architecture` | 1/0 | 16 | 87.82 秒 | 50,321 / ¥0.166581 | 3/40/13/13 ms | 10 | 90，未通过 |
| `draft_xiaojin_hide`<br>`general_run_20260718_005600_6ebb8f` | 完成；`drafting` | 1/0 | 14 | 116.67 秒 | 38,245 / ¥0.136170 | 1/39/12/9 ms | 10 | 90，未通过 |
| `revision_tension`<br>`general_run_20260718_005757_e9d7a3` | 完成；`canon_evidence → revision(失败) → 重规划 → canon_evidence → revision` | 4 个运行记录（当前计划 2）/1 | 16 | 168.92 秒 | 91,526 / ¥0.306114 | 3/117/36/15 ms | 22 | 95，通过 |
| `review_motivation_consistency`<br>`general_run_20260718_010046_9286b7` | 完成；`canon_evidence` | 1/0 | 14 | 81.73 秒 | 36,052 / ¥0.167649 | 1/39/12/4 ms | 10 | 87，未通过 |
| `write_title_authorization`<br>`general_run_20260718_010208_af3791` | 等待人工；`get_novel_structure → update_novel_structure(写入授权)` | 2/0 | 2 | 56.61 秒 | 15,679 / ¥0.056829 | 0/0/0/0 ms | 9 | 90，通过 |

聚合结果：

- 5/8 通过，通过率 62.5%，平均分 91.75。
- 运行记录累计耗时 644.36 秒，单例范围 15.45～168.92 秒。
- 26 次 LLM 调用全部完成；总 Token 302,459，其中输入 263,611、缓存输入 17,664（属于输入子集）、输出 38,848、推理 0。
- 费用均为估算值，合计 ¥1.069332；没有供应商实结费用可用于对账。
- 10 次统一召回共检查 313 个候选、返回 86 个命中，召回层累计 53 ms。
- `plan_xiaojin_conflict` 与 `draft_xiaojin_hide` 均缺少预期的事实证据能力且节点数不足；`review_motivation_consistency` 未选择必要的 `consistency_reviewer` 并漏掉一个答案要点。
- 该基线每例只执行一次，确定性规则得分可复现，但模型规划具有随机性；创作质量仍缺少经过人工校准的语义裁判，不能把本次分数视为稳定置信区间。

## 7. 补充生产样例

| 场景 | 运行与结果 | 关键指标 |
|---|---|---|
| 需要澄清 | `general_run_20260718_010948_924de6`：0 节点，停在 `clarification`，未擅自执行 | 7.95 秒；检查点 3；1 次 LLM；12,685 Token；估算 ¥0.039033 |
| 两节点并行与失败兜底 | `general_run_20260718_010639_c9abba`：`character` 与 `worldbuilding` 具有相同开始时间；后者输入校验失败后重规划为 `canon_evidence` 并完成 | 171.64 秒；检查点 18；1 次重规划；6 次 LLM；65,215 Token；召回 4/79/25/12 ms；估算 ¥0.226575 |
| 中断、重启与恢复 | `general_run_20260718_011115_23cb3c`：执行中重启后恢复为可续跑失败态，调用恢复接口后完成 | 总计 121.29 秒，其中恢复段约 61.42 秒；检查点 17；4 次 LLM；37,069 Token；召回 2/78/2/9 ms；估算 ¥0.130155 |
| 正文预览与写入授权 | `general_run_20260718_012624_c612fc`：`preview_manuscript_patch` 成功，`apply_manuscript_patch` 停在 `write_authorization`，保持未批准 | 22.72 秒；检查点 9；1 次 LLM；14,578 Token；估算 ¥0.048492；正文哈希未变 |

并行样例还暴露出 `worldbuilding.source_request.knowledge_types` 不接受编排器生成的字段；兜底路径成功并不消除首轮 Schema 路由问题。

恢复样例完成后保留了同一 `node_id` 的一条旧 `pending` 记录和一条新 `success` 记录。最终状态与事实输出正确，但节点监控和恢复审计可能误读为仍有悬空节点，应在阶段 06 修复。

正文链路另执行了一次更自然的“读取选区、改写、定位、预览”任务 `general_run_20260718_011713_8a789f`：首轮 `search_manuscript` 的 `excerpt_chars=0` 校验失败，重规划后 `preview_manuscript_patch` 又因无法写入 `operations.0.start_char` 动态绑定而失败，最终运行状态仍记为完成。校正为已知哈希、区间与顶层输出绑定后，上表的写入授权样例成功。说明安全门禁有效，但复杂动态绑定与最终状态判定仍需纳入阶段 06 的长链路可靠性治理。一次使用原始文件字节哈希的无效测试已清理，不计入产品基线。

## 8. 缺口审计与后续阶段映射

| 后续阶段 | 已确认缺口 | 证据摘要 |
|---|---|---|
| `P01` | 统一召回硬化与评测地基 | `RetrievalService` 仍只注入单后端；Mongo 词法后端调用 `list_confirmed_cards()` 后在内存排序；尚无独立召回评测夹具 |
| `P02` | 向量 RAG 独立实验 | 尚无生产 Embedding、向量索引或可重建索引流水线 |
| `P03` | 混合召回接入与灰度 | 尚无词法/向量混合召回、比较门禁或灰度切换 |
| `P04` | 运行时记忆与上下文压缩 | Runtime 尚无 Memory 或 `ContextAssembler`，也无跨轮复用与压缩策略 |
| `P05` | 自适应模型路由 | `ModelRoleRouter` 仍是静态角色到模型映射，没有按质量、成本和失败反馈自适应 |
| `P06` | 检查点恢复可靠性与长链路基准 | JSON 仓储只保存最新运行快照；恢复后存在重复旧节点记录；复杂动态绑定和状态收敛仍不稳定 |
| `P07` | 语义评测与长期运行经验 | 尚无经过人工校准的语义裁判，也没有可治理的长期执行经验资产 |
| `P08` | 业务收口与治理 | 需要在前述能力稳定后收口派生产物保留、脱敏、归档、监控展示和发布清单 |

## 9. 契约、数据与安全边界

- Schema、Protocol、API 和配置：本阶段未修改。
- Markdown：第 82 章标题仍为“第82章 自然玄妙取丹元”，按应用读取语义计算的 SHA-256 仍为 `3a768e062bff2ed33b5ebd93a47f3a66117d342cd8939ec16398c4fb700a1f79`；`git status -- project_assets/source` 为空。
- MongoDB：继续是唯一结构事实源；停库验证确认没有 JSON 回退。
- 作者授权：两条写操作样例都停在 `write_authorization`，未调用批准接口，未发生正文或结构写入。
- 外部访问：所有基线请求均未授权外部访问。
- 敏感信息：报告不保存密钥、完整 Prompt、完整模型响应或正文。`project_assets/derived/` 的本地运行产物可能含模型回答和正文片段，不应整批纳入提交。

## 10. 已知告警与限制

- npm 当前报告 2 个中危依赖告警；未执行可能引入破坏性升级的 `npm audit fix --force`。
- 两个阶段开始前已修改的前端文件存在 LF/CRLF 转换提示，本阶段没有覆盖它们；最终自检还对本次修改的 README、决策文档和三份遥测 JSONL 提示相同的行尾转换风险，但没有空白错误。
- 评测页面的样例选择与历史结果展示未同步。
- 任务包基线脚本假定远程名为 `origin`，不适配当前仓库的 `taichu` 远程名。
- 真实模型费用只有估算值；结果为单次样本，尚无重复运行方差和跨模型比较。
- 当前派生运行记录适合本地审计和回放，不等同于经过脱敏、稳定可提交的评测夹具。

## 11. 回滚

本阶段没有业务代码、Schema、配置、数据库或事实源变更，不需要生产功能回滚。若放弃本次基线，只需撤销本报告及两处资料入口更新，并按运行记录 ID 清理本阶段派生产物；不得触碰阶段开始前的两处前端修改。服务已恢复到固定端口可用状态，MongoDB 知识请求恢复 200。

## 12. 下一阶段可用接口

- `RetrievalService` 与 `RetrievalBackend` 契约可作为阶段 01 的唯一召回演进入口。
- `MongoLexicalRetrievalBackend` 是当前确定性对照组，不应在阶段 01 丢失其回归能力。
- `project_assets/derived/retrieval/calls.jsonl` 已提供候选、命中、耗时和消费方关联字段，可复用为召回评测观测面。
- `general_writing_assistant_core` 的数据集 ID、校验和、五维确定性评分和本报告运行 ID 可用于后续回归比较。
- 通用 Runtime 的 Tool/Subagent Manifest、调用轨迹、人工中断和恢复 API 已可作为后续阶段稳定外层边界。

## 13. 自检与最终结论

- [x] HEAD、分支、远程和工作树已记录。
- [x] 使用当前实际测试数量，没有沿用历史报告数字。
- [x] 后端、前端、固定端口、浏览器和 MongoDB 失败语义均已真实验证。
- [x] 8 个核心样例及补充能力样例均已冻结，失败项未删除或改写为通过。
- [x] 没有修改生产 Prompt、事实源或写入授权结果。
- [x] 报告不包含完整正文、Prompt、模型输出或密钥。
- [x] 后续阶段已按当前代码事实映射。
- [x] 没有创建竞争性的当前架构规则源，没有创建 `.gitkeep`，没有提交或推送。

阶段 00 最终结论：`PASS`。仓库提交与任务包基线一致，基础门禁可复现；当前质量与可靠性缺口已被明确冻结，应从阶段 01 开始按依赖顺序推进，不在阶段 00 内提前实现。
